import { makeStyles } from "@material-ui/styles";

const styles = makeStyles((theme) => ({
  textWrapper: {
    position: "relative",
  },

  largeInput: {
    fontSize: 50,
    width: 60,
  },

  largeInputProps: {
    fontSize: 50,
  },
}));

export default styles;
